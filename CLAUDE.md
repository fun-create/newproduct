# NEW PRODUCT（`/opt/newproduct`）

**共通ルールは HUB `rules/apps-common.md` が正本**（`~/Desktop/Claudcode/bin/hub cat rules/apps-common.md`）。技術方針・配り方・共通アカウント・見た目・品質・連絡の作法はそちらにあり、**ここには書き写さない**。このファイルは**このアプリ固有のこと**だけ。

| ファイル | 何が書いてあるか |
|---|---|
| `REQUIREMENTS.md` | 要件の正本（FR-01〜）。状態は5つ |
| `DECISIONS.md` | なぜそう決めたか。取り消した決定も残してある |
| `HANDOFF.md` | いま動いている状態と宿題。**着手前にここから読む** |

---

## 1. このアプリが担うもの

**商品が生まれるまで。**アイデア → 評価 → 年間プランの枠 → 開発案件 → ゲート（社長・生産部の判断）→ 発売。
既存6アプリは「商品が生まれた後」しか知らない。seisan が商品を認識するのは商品マスタに登録された瞬間から。
**その手前を持つのが本アプリ**（要件定義 第1章）。

やらないこと: 受注データ・実績原価の保持（seisan が正本）／売上計画・予実の正本（keiei が正本）／
LP・商品ページの制作そのもの（Webマーケ部）／モールへの出品操作／FCTR の実行（AutoGrowth）。

## 2. 構成

```
server.py        振り分けだけ。ロジックを置かない
auth.py          **上流 /opt/keiei/app/auth.py の複製。直接編集しない**（§3）
selfcheck.py     全画面の描画検査（80件）。変更のたびに走らせる
migrations/*.sql 名前順に流す。**前に進むだけ。**戻す方向は用意しない
seed/*.tsv       種データ。**data/ の下に置かない**（.gitignore が data/ を丸ごと外す）
app/store.py     DB接続・tx・audit・new_id
app/seed.py      マスタ投入。**何度流しても同じ結果。**起動のたびに流す
app/project.py   案件とカルテ（6節）
app/task.py      タスク・案件外の仕事・月次負荷・ダッシュボード
app/gate.py      G0〜G6。欠落の算出と承認資格
ui/index.html    SPA の外枠。**NAV の正本はここの <script id="fca-nav-data">**
ui/app.js        hash routing と全画面
tools/grant_role.py  業務ロールの割り当て（設定画面ができるまでの代用）
```

**NAV を直すときは `ui/index.html` の JSON を直す。**`app.js` と `selfcheck.py` の両方がそこを読む。
片方だけ直すと、帯と実際の画面が静かにずれる。

## 3. 壊しやすい箇所

### 3-1. `products.name` / `order_lines.product_name` / `message` を読まない

seisan 側のこの3列には、**名入れの入力値に顧客の個人名が混入している**（実例確認済み・N-6-2）。
部分マスクは取りこぼすので、**列そのものを使わない**ことで構造的に防いでいる。

画面に出す商品の表し方は `project.product_label()` の **`cat1 / cat2 / cat3 + size`** だけ。
`internal_name` は社内の呼び名であって顧客データではないので、使ってよい。

**`app.js` 側で分類を組み立て直さないこと。**selfcheck が「商品名の組み立てをしていない」を検査している。
サーバ側1か所に閉じていないと、次に触る人が seisan の列を足してしまう。

### 3-2. `ReadWritePaths` から `config` や `/opt/accounts/state` を外すと、**エラーを出さずに**壊れる

`newproduct.service` の `ProtectSystem=strict` 下では、書ける場所は `ReadWritePaths` だけ。

| 外すと | 何が起きるか |
|---|---|
| `/opt/newproduct/config` | `auth.py` が `.sessions.json` を書けず、**再起動のたびに全員ログアウト**。`svc_token` / `csrf_salt` も毎回作り直しになる |
| `/opt/accounts/state` | ログイン失敗の計数が**このアプリだけ独立**し、凍結がアプリ数ぶん実質緩くなる（共有計数器を入れた理由そのもの） |

**どちらも起動は成功する。**その機能を使うまで気づけない。ユニットを触ったら、
ログアウトが起きないこと・`/opt/accounts/state` に書けることを実際に確かめる。

### 3-3. `/assets/` の Content-Type は許可リスト。**状態コードだけ見ても分からない**

`serve_static()` は拡張子が `ASSET_TYPES` に無ければ、ファイルが存在しても 404 を返す。
新しい種類の素材（`.woff2` など）を置いても、**追加しない限り配られない**。

逆に言うと、**200 が返っていても Content-Type が違えばブラウザは描かない。**
`curl -I` の状態コードだけで確認を終えないこと。`content-type` の行まで見る。
`ui/assets/allowlist-probe.txt` は、許可外拡張子が 404 になることを確かめるための置き石。消さない。

### 3-4. 「分からない」を 0 で埋めない

| 対象 | いまの状態 |
|---|---|
| `flow_type.effort_point` の **`material`（⑤資材リニューアル）** | **NULL。**2026年度の年間プランに1件も使われておらず実測できない。0 にすると「工数ゼロの安いフロー」に見えて逆に危ない |
| **`pagerenew`（⑦ページリニューアル）の標準タスク** | **0行。**178行の種データに1行も無い。`has_template=0`。案件を作るとタスク一覧が空になる |

**どちらも埋めない。発明しない。**画面には「未確定」「標準タスク未定義」と、理由つきで出す。
`selfcheck.py` が「係数が未確定の開発タイプが1つ」であることを検査している。
**埋めると、この検査が落ちる。**それは検査の誤りではなく、埋めたほうが誤り。

### 3-5. 「43.25h／27.28h」は**6フロー合算**であって1本あたりではない

管理者ロールの標準工数。**1本の商品を作るのに43時間かかる、という意味ではない。**
6つの開発フローのテンプレートを全部足した値。

要件定義書自身が一度これを取り違えて訂正を入れている（設計書 §5-6）。
`task.template_totals()` はこの注記を必ず返し、`selfcheck.py` が
「6フロー合算」「1本あたりではない」の文言が画面に出ることを検査している。
**この文言を消さないこと。**数字だけが独り歩きする。

同じ理由で、ロール `webmkt` / `prod` の **0h は「実際に0時間」ではなく「試算から意図的に除外した」値**
（他部署なので商品開発部のAI削減試算に入れていない）。自部署の工数と足さない。

### 3-6. `auth.py` は上流の複製。直接編集しない

上流は `/opt/keiei/app/auth.py`（さらにその上流は lpscope）。
ローカル変更は `_upstream.json` の `local_edits` に書いた4点だけ:
**出どころのコメント／`CONFIG` の置き場／`SESSION_COOKIE`／`CSRF_HEADER`**。

```bash
sudo -u newproduct python3 -m unittest tests.test_upstream -v
```

これが落ちたら、**上流が変わったか、ここで余計な編集をしたか**のどちらか。
直したいときは上流（keiei セッション）へ照会する。**片方だけ直すと静かにズレる。**
上流が読めない環境では skip する。**skip は緑に混ざるので、件数で読むこと**（共通ルール §7）。

## 4. ドメイン固有の判断

根拠は `DECISIONS.md`。ここは「知らないと誤解する」ものだけ。

- **アプリ権限（`admin`/`user`）と業務ロール（`president`/`prod`…）は別軸**（ADR-005）。
  ゲートの承認資格は業務ロールだけで決まる。**`admin` でも G3（社長）は通せない。**
  割り当てが1件も無いあいだは誰もどのゲートも通せない。**それで正しい**
- **④ニューモデル追加は G2 → G5 の簡易フロー**（ADR-011）。G1・G3・G4 は「未達」ではなく **`対象外`**。
  空欄のまま放置すると永久に未達に見えるため、状態として持っている
- **欠落があるゲートは `通過` させない**（ADR-009）。差戻し・保留・中止は欠落があっても記録できる
- **保留・中止には選択式の理由が要る**（`hold_reason` / `abort_reason`）。自由記述だけだと後から数えられない
- **ページリニューアルは既定で新商品売上に含めない**（ADR-003）。計画の38%がこれなので、
  既定で含めると「新商品売上」も「月3商品」の本数も意味を失う。含める案件は**全額／増分**を必ず選ぶ
- **タスクの期限は自動で入れない**（ADR-012）。営業日マスタは calfc が正本で連携が未依頼。
  推測日付を入れると、実運用の「期限空欄88件」という問題そのものが見えなくなる
- **工数はタスク実施月に積む**（発売月ではない）。期限も開始日も無い行は、どの月にも積まず別に数える
- **Drive とアプリの併存中は `source_of_truth` が正本を決める。**`drive` の案件はアプリ側で判定させない

## 5. 確かめるときの根拠

**このアプリにはリポジトリがまだ無い**（`registry/apps.json` の `gap`）。
`/opt/newproduct` が唯一の正本で、`git fetch origin` で確かめる先が存在しない。
**根拠は常に本番の実物**にすること。**行番号を引かない。**
「ファイルが無い」は気づけるが、行番号だけズレる形は気づけない（共通ルール §1）。

```bash
ssh -i ~/.ssh/lpscope_vps masateru@162.43.43.186
# ディレクトリが 750 なので、glob もリダイレクトも sudo sh -c で囲む
sudo sh -c 'grep -n "def svc_ok" /opt/newproduct/server.py'
sudo sh -c 'wc -l /opt/newproduct/app/*.py' 
```

`deploy/` は正本ではない。systemd の正本は `/etc/systemd/system/`、cron は `/etc/cron.d/`。
配る前に `diff -u` で突き合わせる。
