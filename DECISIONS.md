# NEW PRODUCT 決定の記録

なぜそう決めたか。**取り消した決定も消さずに残す。**
消すと「一度そう考えて、やめた」という事実ごと消え、同じ議論を繰り返します。

書く形: 何を → 何に ／ なぜ ／ 影響する要件ID ／ 取り消した決定があればその番号。
**推測を書かない。**実機で確かめた結果だけを書く（共通ルール §7）。

---

## ADR-001 — URL とポート（2026-09-20）

**決定**: `newproduct.fun-create.co.jp` ／ ポート **8794**。

**なぜ**: 既存6アプリが 8788〜8793 を連番で使っている（lpscope 8788 / autogrowth 8789 /
calendar 8790 / cip 8791 / business-analysis 8792 / ai-assistant 8793）。
**7つ目として次の番号を取る**のが、調べなくても分かる唯一の規則。
ドメインも `<アプリ名>.fun-create.co.jp` の既存の形に合わせた。

**影響**: FR-08・FR-11
**根拠**: 要件定義 第1章の表 ／ `deploy/newproduct.service` の `NEWPRODUCT_PORT=8794`

---

## ADR-002 — FCTR を本アプリに取り込まず AutoGrowth へ（2026-09-20）

**決定**: FCTR（トレンド収集・スコアリング）の**実行基盤は AutoGrowth**。
本アプリは週次のTOP3・最優先1案・判断理由・次アクションを**受け取る側**に回る。

**当初案（取り下げ）**: F-9-1 は当初「**FCTR週次分析を本アプリに移す**」だった。
要件定義の初稿はスクレイピング・API収集・スコアリングを本アプリの機能として数えていた。
**これは取り下げた。**

**なぜ取り下げたか**:
1. 十文字さんの指示（2026-09-20）「FCTR は精度的にブラッシュアップとアップデートが必要。
   設計から見直し、VPS上のできれば AutoGrowth 上で実行する」
2. 監査の結果、**「移設」では済まないことが分かった**。収集7本のうち6本が合成データで、
   乱数の種が実行ごとに変わるため週次の増減が再現しない。データ収集から作り直しになる
   （`docs/product-dev/2026-09-20_FCTR_監査と再設計.md`）

**影響**: FR-135〜FR-139 が「受け取る側」の要件に変わった。FR-150 を `見送り` として残す
**根拠**: 要件定義 F-9 の方針ブロック ／ 設計書 §3-2

---

## ADR-003 — ページリニューアルは売上に既定で含めない（2026-09-20）

**決定**: 案件ごとに「売上計上に含めるか」のフラグを持つ。
**ページリニューアルは既定で「含めない」。**大幅リニューアルの場合だけ案件単位で選べる。
含める案件は計上方式（**全額** か **増分**）も選び、**どちらを選んだかを必ず記録する**。

**なぜ**: 2026年度の計画26件のうち **10件（38%）がページリニューアル**。
既定で含めてしまうと「新商品売上」も「月3商品」の本数も意味を持たなくなる。
逆に一律で除くと、大幅リニューアルの売上が誰の成果にもならない。
**既定を安全側（含めない）に置き、例外を案件単位で明示させる**形にした。

**実装**: `project.revenue_counted` の既定は 0。`revenue_counted=1` で方式未選択なら
`project.create()` が例外を投げる（「選べる」を「選ばなくても通る」にしない）。

**影響**: FR-34・FR-35・FR-112・FR-155
**根拠**: 2026-09-20 十文字さん決定 ／ 要件定義 F-4-9・F-10-11

---

## ADR-004 — ダッシュボード1段目 ＝ 「いま詰まっているもの」（2026-09-21）

**決定**: 1段目は **期限切れタスク ／ 自分のゲート待ち ／ 今日のタスク**。
コンセプト在庫月数は2段目へ。

**当初案（変更した）**: `app-ui` の画面設計は **1段目に「コンセプト在庫月数」**を置いていた。

**なぜ変えたか**: コンセプト在庫月数は**月次の問い**である。毎朝開く人（商品開発部の
管理者・メンバー）が最初に要るのは「**何をすればいいか**」。実運用の台帳は
**未着手256件・期限空欄88件**で、痛点はそこにある。
R-5（データ入稿進行管理表は17シート中14枚が空のまま放置された）を踏まえると、
**最初の画面が日々の役に立たないとアプリごと使われなくなる。**
コンセプト在庫月数は捨てず、経営の問いとして2段目に残した。

**影響**: FR-62（実装済）・FR-63（未着手）
**根拠**: 設計書 §10-2 ① ／ 十文字さん「お任せで進めてください」

---

## ADR-005 — ロールと権限を2軸に分ける（2026-09-21）

**決定**: **アプリ権限**（`config/users.json` の `role` ＝ `admin`/`user`）と
**業務ロール**（`role_member` ＝ 社長／管理者／メンバー／商品開発部／Webマーケ／生産部／パート）
を別の軸として持つ。**ゲートの承認資格はアプリ権限で決めない。**

**なぜ**: 2値の `admin`/`user` にゲート承認を載せると、
**承認させたいだけの社長を admin にする**か、**全ての管理者に社長の承認を許す**かの
どちらかになる。どちらも誤り。現行フローでも「社長の確認」と「生産部の確定」は
別人格の判断で、それを型で表す。`admin` は設定・マスタ・テンプレートを触れる人であって、
承認とは別の話。

**実装**: `gate.can_approve()` は `role_member` だけを見る。アプリ権限を一切参照しない。
`gate_def.approver_role` は業務ロールの配列。

**影響**: FR-57 ／ HANDOFF の「`prod` 未割り当てで G4 が通せない」はこの決定の帰結
**根拠**: 設計書 §10-2 ②

---

## ADR-006 — 実装順序は第2段から（2026-09-21）

**決定**: **第2段 → 第1段 → 第3段 → 第4段 → 第5段**。
案件・タスク・ゲートを先に作る。

**両論あった**（要件定義 第9章・設計書 §10 が両方を書いている）:

| | 第1段（アイデア・採点）を先に | 第2段（案件・タスク・ゲート）を先に |
|---|---|---|
| 理由 | 採点設計の是正は**アイデアが積み上がるほどやり直しが高くつく**。オリジナル750件の95%がCランクで、推し活うちわとは分布も閾値も違い横並びに比較できない。入口が詰まったまま案件を作っても流れない | **置き換える既存作業が最大**（7枚の複製シート・49ファイル・397件の台帳）。R-5「入力が増えるだけの機能は放置される」に最も陥りにくい |
| 推した立場 | 業務要件（`ec-product`）・全体設計書 本文 | **画面設計（`app-ui`）** |

**なぜ第2段を採ったか**: `app-ui` の指摘に重みがある。データ入稿進行管理表は
17シート中14枚が空のまま放置された。**最初に作るものが「既存作業の置き換え」でないと、
アプリ自体が信用されない。**
採点設計の是正を後ろにしても、**再採点しない方針（rubric の version 分離）**を
既に決めているため、アイデアが積み上がっても後から version を分けて直せる。
**先送りの損が小さいほうを後ろにした。**

**影響**: 第2段の FR-28〜FR-64 が先に `実装済` になり、第1段の FR-65〜FR-87 が `未着手`
**根拠**: 設計書 §10-2 ③

---

## ADR-007 — `.fca-logo` の切り取りを廃止（2026-09-21）

**決定**: `.fca-logo` は**切り取らない**。高さの上限（`--fca-logo-h`）だけ決め、
幅は絵の比に任せる（`width:auto` / `max-width:100%`）。`--fca-logo-w` は宣言ごと削除。

**当初の案（欠陥があった。修正済み）**: こちらから出した最初の案は
「canvas は全アプリ 1200x240 の 5:1 で、右側は空白」という前提で絵を切り詰める
`--fca-logo-w` を残したまま、**`--fca-logo-h` を下げていなかった。**
その結果、**ロゴが従来より25%大きく出る**という欠陥があった。

**Auto GROWTH の指摘で修正した**内容:
1. 実物の canvas 幅は **479〜1436 とアプリごとに違い**、紺の下地が全面を覆っていて空白が無い。
   切り詰めた結果、見えていたのは lpscope 45% / cip 7% など（**8アプリ全滅**）
2. 高さの上限を**旧40px の 0.8倍＝32px**（`.fca-logo-lg` は 旧描画90px＝見える72px）に
   下げて初めて、従来と同じ大きさになる

**なぜ宣言ごと消したか**: **効かない調整つまみを残さない。**
値が残っていると、次に触る人が「幅で調整できる」と誤解する。

**影響**: FR-10（本番のログイン画面で目視して `検証済` にした唯一の見た目の要件）
**根拠**: `ui/assets/app-shell.css` 156〜162行・318〜341行 ／ 2026-09-21 ブラウザで実測

---

## ADR-008 — `project_check`（人が確認した記録）を足す（2026-09-21）

**決定**: ゲートの必須項目のうち**このアプリがまだ持っていないデータ**（試算原価・年間目標・
商品コードなど、第3段／第4段で入るもの）について、**人が確認したという記録**を
`project_check` として持つ。誰が・いつ確認したかを必ず残す。

**設計書に無い判断。**設計書 §4-6 はゲートの必須項目を `gate_def.required_items` に
持つところまでしか決めていない。

**なぜ**: 第2段だけでは required_items の半分が「アプリの中に無いデータ」を指す。
選択肢は3つあった。

1. 無いものは検査しない → **`required_items` が飾りになる**
2. 無いものがあると永久に通せない → **第2段が実務で使えない**
3. **人が確認した記録を残して通す** ← これを採った

「無い」を「有る」に化けさせないために、**チェックそのものではなく「誰がいつ確認したか」**を
記録する。第3段・第4段でデータが入ったら、`check` の種別を `manual` から
自動判定（`section:` / `project:` など）へ差し替える。

**影響**: FR-61・FR-59・FR-60
**根拠**: `migrations/003_project.sql` の `project_check` ／ `app/seed.py` の `_item(..., "manual", ...)`

---

## ADR-009 — 欠落があるゲートは「通過」させない（2026-09-21）

**決定**: `required_items` に欠落がある状態では `通過` を**拒否する**（例外を投げる）。
**差戻し・保留・中止は、欠落があっても記録できる**（むしろ欠けているから止める）。

**設計書に無い判断。**要件定義 F-6 の表は「通過条件（揃わないと通さない）」と書いているが、
**通せてしまった場合にどうするか**は書かれていない。

**なぜ**: 通せてしまうと `required_items` は飾りになり、
**B-3（何が揃えば完成かの定義が無い）を新アプリの中で再生産する。**
警告だけにして人の判断に委ねる案もあったが、F-3-3（年間プランの挿入ルールは
「警告として出す。ブロックはしない」）と違い、**ゲートは「揃わないと通さない」と
要件に明記されている。**警告とブロックの使い分けは要件が区別しているので、それに従った。

**影響**: FR-56
**根拠**: `app/gate.py` の `review()` ／ 要件定義 F-6 の表

---

## ADR-010 — G2 の承認者は「管理者 または 社長」どちらでも可（2026-09-21）

**決定**: G2（年間プラン採択）の `approver_role` は `["admin", "president"]`。
**どちらの資格でも記録できる。**両方の承認を求めない。

**設計書に無い判断。**要件定義 F-6 の表は G2 の「誰が」を **「管理者 ＋ 社長」**と
書いている。この「＋」を **AND（両者の承認が要る）**と読むか
**OR（どちらの資格でも通せる）**と読むかが決まっていなかった。

**なぜ OR を採ったか**:
1. AND にすると、**2段階承認という仕組みそのもの**（承認の待ち行列・片方だけ済んだ状態・
   片方の差戻し）が要る。第2段の範囲を大きく超える
2. G3（コンセプト承認）が **`["president"]` の単独**で、F-6-1 が
   「枠を取る判断（G2）と中身の判断（G3）を分ける」と言っている。
   **社長の意思は G3 で必ず1回入る。**G2 で二重に要求する必要が薄い
3. `gate_review.approved_role`（**どの業務ロールとして通したか**）を記録しているので、
   後から「G2 を管理者が通した案件」を数えられる。**AND が要ると分かったら、
   そのデータを見てから決められる**

**取り消す可能性がある。**運用してみて「社長の承認が G2 に要る」と分かったら AND へ直す。
そのときこの ADR を取り消し線ではなく**新しい ADR で上書き**する。

**影響**: FR-52・FR-57
**根拠**: `app/seed.py` の `GATES` の G2 ／ 要件定義 F-6 の表

---

## ADR-011 — ④の対象外は G1・G3・G4 の3つ（2026-09-21）

**決定**: ④ニューモデル追加（スキンシール）の簡易フローで `対象外` にするゲートは
**G1・G3・G4 の3つ**。G0・G2・G5・G6 は通す。

**設計書に無い判断。**F-6-4 は「**G2 → G5 だけを通す簡易フロー**」と書いている。
字面どおりなら G0・G6 も外れるが、それだと

- **G0（起票）を外す** → 起票が記録に残らない。商品案名も概要も無い案件ができる
- **G6（発売後評価）を外す** → 機種追加の売上が評価されない

**なぜこの切り方か**:
- **G0 は通す。**起票はすべての案件の入口で、F-6 の表でも「起票は軽いままにする」
  とあるだけで、④を除外していない
- **G1（評価通過）は対象外。**既存商品の機種追加なので、採点・重複チェックの対象ではない
- **G3（コンセプト承認）は対象外。**現行フローにも**④には「社長の確認」が存在しない**
  （要件定義 F-6 の前書きで実査済み）
- **G4（生産可否確定）は対象外。**生産方法は本体商品で確定済み
- **G6 は通す。**これは要件定義 F-6-4 の字面を**広げた**判断。売上の評価を外す理由が無い。
  もし「④は発売後評価も不要」であれば、`applies_to_flow_types` から外すだけで直る

**`対象外` は「未達」ではない。**空欄のままだと永久に未達に見えるため、
状態として `対象外`（記号 `/`）を持たせ、画面に「放置ではなく設計です」と出している。

**影響**: FR-54
**根拠**: `app/seed.py` の `NOT_NEWMODEL` ／ `app/gate.py` の `applies()` ／ 設計書 §5-6

---

## ADR-012 — タスクの期限を自動割付しない（2026-09-21）

**決定**: テンプレート展開時に**開始日・期限を入れない。**期限は人が入れる。

**要件は割付を求めている。**F-5-2 は「営業日マスタを使って開始日・期限を自動割付」。
**これを第2段では実装しない**と決めた。

**なぜ**:
1. **営業日マスタは calfc が正本**で、その連携（設計書 第11章 ⑧ `service_tokens.json` への
   追加）が**まだ依頼できていない**。本アプリ側に営業日を持つと N-12（マスタは1箇所）を破る
2. **推測で日付を入れると、実運用の「期限空欄88件」が見えなくなる。**
   空欄は問題の在処であって、埋めれば消える種類の問題ではない。
   自動で入れた日付は「誰も決めていない期限」で、守られない

**いま起きること**: 案件を作るとタスクは全件「期限なし」で並ぶ。
タスク画面には**「期限なし」を常設ボタン**として置き、件数が見えるようにしてある。

**影響**: FR-41（未着手のまま残す）・FR-45
**根拠**: `app/project.py` の `expand_tasks()` ／ `tests/e2e_http.py`「期限なしで32件」

---

## ADR-013 — 旧テンプレート（190行）を採用しない（2026-09-21）

**決定**: フローテンプレートの正本は
`03_商品開発フローAI効率化分析__タスク別AI適用一覧.tsv` の **178行**。
`01_進捗管理商品開発__テンプレート.tsv`（タスク190行）は**採用しない。差分は記録のみ。**

**なぜ**: 2つの表は既に乖離している（要件定義 2-1 が「178 ／ 196 ← **既に乖離**」と実査）。
`03` 側は担当ロール・標準工数・AI適用カテゴリ・削減率を持ち、
**本アプリが要る列がすべて揃っている。**`01` は古い。

**ただし「予備時間」の行は `01` にしか無い。**F-5-7 が明示的に「持て」と言っている項目なので、
**値だけ `seed.old_reserve_hours()` に記録してある**（`task_template` には入れていない）。
採否は商品開発部の判断（HANDOFF の宿題）。

**「196行」という数え方について**: 各フローの「商品名」見出し行6件を含む数え方。
タスクの行は190件。**件数を引用するときはどちらの数え方かを添える。**

**影響**: FR-39・FR-46・FR-154
**根拠**: `app/seed.py` の `diff_report()` ／ 要件定義 2-1

---

## ADR-014 — `/opt/newproduct` を 750 にした（2026-09-21）

**決定**: `/opt/newproduct` の所有者は `newproduct:newproduct`、**パーミッションは 750**。
`config/` `logs/` `migrations/` `seed/` `tests/` `app/` も 750（`drwxr-x---`）。

**なぜ 755 ではないか**:
- `config/` には `users.json`（scrypt ハッシュ）・`.sessions.json`・`svc_token` が入る。
  **同じサーバー上の他アプリのユーザー**（lpscope・autogrowth・calfc・seisan・keiei）から
  読めてよいものではない
- 6アプリは `funcreate-auth` グループを共有している（FR-04）。
  **グループが同じということは、755 にすると全アプリから読める**ということ。
  共通ルール §4 の「グループに w を足さない」と同じ理由で、r も絞る
- `newproduct` ユーザー自身は問題なく読める。systemd の `User=newproduct` で走る

**副作用（把握している）**: root 以外から `ls /opt/newproduct/app/*.py` のような
glob が通らない。調査時は `sudo sh -c "..."` で囲む必要がある。
**これは不便であって欠陥ではない**ので、受け入れる。

**影響**: FR-14・FR-24
**根拠**: 設計書 第11章 ① ／ 2026-09-21 `ls -la` で実測（uid 988・750）

---

## ADR-015 — 未実装のサーバ間APIは 501 を返す（2026-09-21）

**決定**: `/api/svc/plan` `/api/svc/themes` `/api/svc/ingest` は、実装前の段階で
**404 ではなく 501** を返す（`contract_version` を添えて）。

**なぜ**: 404 にすると、呼ぶ側が**「経路が無い（綴り間違い）」と「まだ無い（未実装）」を
区別できない。**相手セッション（AutoGrowth・keiei・calfc）が連携を試したときに、
自分の側の間違いを疑って時間を使うことになる。

**影響**: FR-27
**根拠**: `server.svc()` ／ 設計書 §6

---

## ADR-016 — 分からないものを 0 で埋めない（2026-09-21）

**決定**: 次の2つは **NULL のまま**入れ、画面に「未確定」と出す。

| 対象 | なぜ 0 にしないか |
|---|---|
| `flow_type.effort_point` の ⑤資材リニューアル | 2026年度の年間プランに1件も使われておらず**実測できない**。0 で埋めると「工数ゼロの安いフロー」に見え、**逆に危ない** |
| ⑦ページリニューアルの標準タスク | 178行に1行も無い。**発明しない。**計画の38%を占めながら定義が無いこと自体を画面に出す |

**なぜ**: 共通ルール §7「**根拠のない数値を出さない**」「**『無い』と『0』を混同しない**」。
埋めた瞬間に、**それが推測だったことが誰にも分からなくなる。**

**影響**: FR-21・FR-49・FR-87
**根拠**: `app/seed.py` の `FLOW_TYPES` `FLOW_NOTE` ／ `selfcheck.py`（係数未確定が1つ、を検査している）
