# NEW PRODUCT 決定の記録

なぜそう決めたか。**取り消した決定も消さずに残す。**
消すと「一度そう考えて、やめた」という事実ごと消え、同じ議論を繰り返します。

書く形: 何を → 何に ／ なぜ ／ 影響する要件ID ／ 取り消した決定があればその番号。
**推測を書かない。**実機で確かめた結果だけを書く（共通ルール §7）。

---

## ADR-001 — URL とポート（2026-09-20）

**決定**: `newproduct.fun-create.co.jp` ／ ポート **8794**。

**なぜ**: 既存6アプリが 8788〜8793 を連番で使っている（lpscope 8788 / autogrowth 8789 /
calendar 8790 / cip 8791 / business-analysis 8792 / ai-assistant 8793）。
**7つ目として次の番号を取る**のが、調べなくても分かる唯一の規則。
ドメインも `<アプリ名>.fun-create.co.jp` の既存の形に合わせた。

**影響**: FR-08・FR-11
**根拠**: 要件定義 第1章の表 ／ `deploy/newproduct.service` の `NEWPRODUCT_PORT=8794`

---

## ADR-002 — FCTR を本アプリに取り込まず AutoGrowth へ（2026-09-20）

**決定**: FCTR（トレンド収集・スコアリング）の**実行基盤は AutoGrowth**。
本アプリは週次のTOP3・最優先1案・判断理由・次アクションを**受け取る側**に回る。

**当初案（取り下げ）**: F-9-1 は当初「**FCTR週次分析を本アプリに移す**」だった。
要件定義の初稿はスクレイピング・API収集・スコアリングを本アプリの機能として数えていた。
**これは取り下げた。**

**なぜ取り下げたか**:
1. 十文字さんの指示（2026-09-20）「FCTR は精度的にブラッシュアップとアップデートが必要。
   設計から見直し、VPS上のできれば AutoGrowth 上で実行する」
2. 監査の結果、**「移設」では済まないことが分かった**。収集7本のうち6本が合成データで、
   乱数の種が実行ごとに変わるため週次の増減が再現しない。データ収集から作り直しになる
   （`docs/product-dev/2026-09-20_FCTR_監査と再設計.md`）

**影響**: FR-135〜FR-139 が「受け取る側」の要件に変わった。FR-150 を `見送り` として残す
**根拠**: 要件定義 F-9 の方針ブロック ／ 設計書 §3-2

---

## ADR-003 — ページリニューアルは売上に既定で含めない（2026-09-20）

**決定**: 案件ごとに「売上計上に含めるか」のフラグを持つ。
**ページリニューアルは既定で「含めない」。**大幅リニューアルの場合だけ案件単位で選べる。
含める案件は計上方式（**全額** か **増分**）も選び、**どちらを選んだかを必ず記録する**。

**なぜ**: 2026年度の計画26件のうち **10件（38%）がページリニューアル**。
既定で含めてしまうと「新商品売上」も「月3商品」の本数も意味を持たなくなる。
逆に一律で除くと、大幅リニューアルの売上が誰の成果にもならない。
**既定を安全側（含めない）に置き、例外を案件単位で明示させる**形にした。

**実装**: `project.revenue_counted` の既定は 0。`revenue_counted=1` で方式未選択なら
`project.create()` が例外を投げる（「選べる」を「選ばなくても通る」にしない）。

**影響**: FR-34・FR-35・FR-112・FR-155
**根拠**: 2026-09-20 十文字さん決定 ／ 要件定義 F-4-9・F-10-11

---

## ADR-004 — ダッシュボード1段目 ＝ 「いま詰まっているもの」（2026-09-21）

**決定**: 1段目は **期限切れタスク ／ 自分のゲート待ち ／ 今日のタスク**。
コンセプト在庫月数は2段目へ。

**当初案（変更した）**: `app-ui` の画面設計は **1段目に「コンセプト在庫月数」**を置いていた。

**なぜ変えたか**: コンセプト在庫月数は**月次の問い**である。毎朝開く人（商品開発部の
管理者・メンバー）が最初に要るのは「**何をすればいいか**」。実運用の台帳は
**未着手256件・期限空欄88件**で、痛点はそこにある。
R-5（データ入稿進行管理表は17シート中14枚が空のまま放置された）を踏まえると、
**最初の画面が日々の役に立たないとアプリごと使われなくなる。**
コンセプト在庫月数は捨てず、経営の問いとして2段目に残した。

**影響**: FR-62（実装済）・FR-63（未着手）
**根拠**: 設計書 §10-2 ① ／ 十文字さん「お任せで進めてください」

---

## ADR-005 — ロールと権限を2軸に分ける（2026-09-21）

**決定**: **アプリ権限**（`config/users.json` の `role` ＝ `admin`/`user`）と
**業務ロール**（`role_member` ＝ 社長／管理者／メンバー／商品開発部／Webマーケ／生産部／パート）
を別の軸として持つ。**ゲートの承認資格はアプリ権限で決めない。**

**なぜ**: 2値の `admin`/`user` にゲート承認を載せると、
**承認させたいだけの社長を admin にする**か、**全ての管理者に社長の承認を許す**かの
どちらかになる。どちらも誤り。現行フローでも「社長の確認」と「生産部の確定」は
別人格の判断で、それを型で表す。`admin` は設定・マスタ・テンプレートを触れる人であって、
承認とは別の話。

**実装**: `gate.can_approve()` は `role_member` だけを見る。アプリ権限を一切参照しない。
`gate_def.approver_role` は業務ロールの配列。

**影響**: FR-57 ／ HANDOFF の「`prod` 未割り当てで G4 が通せない」はこの決定の帰結
**根拠**: 設計書 §10-2 ②

---

## ADR-006 — 実装順序は第2段から（2026-09-21）

**決定**: **第2段 → 第1段 → 第3段 → 第4段 → 第5段**。
案件・タスク・ゲートを先に作る。

**両論あった**（要件定義 第9章・設計書 §10 が両方を書いている）:

| | 第1段（アイデア・採点）を先に | 第2段（案件・タスク・ゲート）を先に |
|---|---|---|
| 理由 | 採点設計の是正は**アイデアが積み上がるほどやり直しが高くつく**。オリジナル750件の95%がCランクで、推し活うちわとは分布も閾値も違い横並びに比較できない。入口が詰まったまま案件を作っても流れない | **置き換える既存作業が最大**（7枚の複製シート・49ファイル・397件の台帳）。R-5「入力が増えるだけの機能は放置される」に最も陥りにくい |
| 推した立場 | 業務要件（`ec-product`）・全体設計書 本文 | **画面設計（`app-ui`）** |

**なぜ第2段を採ったか**: `app-ui` の指摘に重みがある。データ入稿進行管理表は
17シート中14枚が空のまま放置された。**最初に作るものが「既存作業の置き換え」でないと、
アプリ自体が信用されない。**
採点設計の是正を後ろにしても、**再採点しない方針（rubric の version 分離）**を
既に決めているため、アイデアが積み上がっても後から version を分けて直せる。
**先送りの損が小さいほうを後ろにした。**

**影響**: 第2段の FR-28〜FR-64 が先に `実装済` になり、第1段の FR-65〜FR-87 が `未着手`
**根拠**: 設計書 §10-2 ③

---

## ADR-007 — `.fca-logo` の切り取りを廃止（2026-09-21）

**決定**: `.fca-logo` は**切り取らない**。高さの上限（`--fca-logo-h`）だけ決め、
幅は絵の比に任せる（`width:auto` / `max-width:100%`）。`--fca-logo-w` は宣言ごと削除。

**当初の案（欠陥があった。修正済み）**: こちらから出した最初の案は
「canvas は全アプリ 1200x240 の 5:1 で、右側は空白」という前提で絵を切り詰める
`--fca-logo-w` を残したまま、**`--fca-logo-h` を下げていなかった。**
その結果、**ロゴが従来より25%大きく出る**という欠陥があった。

**Auto GROWTH の指摘で修正した**内容:
1. 実物の canvas 幅は **479〜1436 とアプリごとに違い**、紺の下地が全面を覆っていて空白が無い。
   切り詰めた結果、見えていたのは lpscope 45% / cip 7% など（**8アプリ全滅**）
2. 高さの上限を**旧40px の 0.8倍＝32px**（`.fca-logo-lg` は 旧描画90px＝見える72px）に
   下げて初めて、従来と同じ大きさになる

**なぜ宣言ごと消したか**: **効かない調整つまみを残さない。**
値が残っていると、次に触る人が「幅で調整できる」と誤解する。

**影響**: FR-10（本番のログイン画面で目視して `検証済` にした唯一の見た目の要件）
**根拠**: `ui/assets/app-shell.css` 156〜162行・318〜341行 ／ 2026-09-21 ブラウザで実測

---

## ADR-008 — `project_check`（人が確認した記録）を足す（2026-09-21）

**決定**: ゲートの必須項目のうち**このアプリがまだ持っていないデータ**（試算原価・年間目標・
商品コードなど、第3段／第4段で入るもの）について、**人が確認したという記録**を
`project_check` として持つ。誰が・いつ確認したかを必ず残す。

**設計書に無い判断。**設計書 §4-6 はゲートの必須項目を `gate_def.required_items` に
持つところまでしか決めていない。

**なぜ**: 第2段だけでは required_items の半分が「アプリの中に無いデータ」を指す。
選択肢は3つあった。

1. 無いものは検査しない → **`required_items` が飾りになる**
2. 無いものがあると永久に通せない → **第2段が実務で使えない**
3. **人が確認した記録を残して通す** ← これを採った

「無い」を「有る」に化けさせないために、**チェックそのものではなく「誰がいつ確認したか」**を
記録する。第3段・第4段でデータが入ったら、`check` の種別を `manual` から
自動判定（`section:` / `project:` など）へ差し替える。

**影響**: FR-61・FR-59・FR-60
**根拠**: `migrations/003_project.sql` の `project_check` ／ `app/seed.py` の `_item(..., "manual", ...)`

---

## ADR-009 — 欠落があるゲートは「通過」させない（2026-09-21）

**決定**: `required_items` に欠落がある状態では `通過` を**拒否する**（例外を投げる）。
**差戻し・保留・中止は、欠落があっても記録できる**（むしろ欠けているから止める）。

**設計書に無い判断。**要件定義 F-6 の表は「通過条件（揃わないと通さない）」と書いているが、
**通せてしまった場合にどうするか**は書かれていない。

**なぜ**: 通せてしまうと `required_items` は飾りになり、
**B-3（何が揃えば完成かの定義が無い）を新アプリの中で再生産する。**
警告だけにして人の判断に委ねる案もあったが、F-3-3（年間プランの挿入ルールは
「警告として出す。ブロックはしない」）と違い、**ゲートは「揃わないと通さない」と
要件に明記されている。**警告とブロックの使い分けは要件が区別しているので、それに従った。

**影響**: FR-56
**根拠**: `app/gate.py` の `review()` ／ 要件定義 F-6 の表

---

## ADR-010 — G2 の承認者は「管理者 または 社長」どちらでも可（2026-09-21）

**決定**: G2（年間プラン採択）の `approver_role` は `["admin", "president"]`。
**どちらの資格でも記録できる。**両方の承認を求めない。

**設計書に無い判断。**要件定義 F-6 の表は G2 の「誰が」を **「管理者 ＋ 社長」**と
書いている。この「＋」を **AND（両者の承認が要る）**と読むか
**OR（どちらの資格でも通せる）**と読むかが決まっていなかった。

**なぜ OR を採ったか**:
1. AND にすると、**2段階承認という仕組みそのもの**（承認の待ち行列・片方だけ済んだ状態・
   片方の差戻し）が要る。第2段の範囲を大きく超える
2. G3（コンセプト承認）が **`["president"]` の単独**で、F-6-1 が
   「枠を取る判断（G2）と中身の判断（G3）を分ける」と言っている。
   **社長の意思は G3 で必ず1回入る。**G2 で二重に要求する必要が薄い
3. `gate_review.approved_role`（**どの業務ロールとして通したか**）を記録しているので、
   後から「G2 を管理者が通した案件」を数えられる。**AND が要ると分かったら、
   そのデータを見てから決められる**

**取り消す可能性がある。**運用してみて「社長の承認が G2 に要る」と分かったら AND へ直す。
そのときこの ADR を取り消し線ではなく**新しい ADR で上書き**する。

**影響**: FR-52・FR-57
**根拠**: `app/seed.py` の `GATES` の G2 ／ 要件定義 F-6 の表

---

## ADR-011 — ④の対象外は G1・G3・G4 の3つ（2026-09-21）

**決定**: ④ニューモデル追加（スキンシール）の簡易フローで `対象外` にするゲートは
**G1・G3・G4 の3つ**。G0・G2・G5・G6 は通す。

**設計書に無い判断。**F-6-4 は「**G2 → G5 だけを通す簡易フロー**」と書いている。
字面どおりなら G0・G6 も外れるが、それだと

- **G0（起票）を外す** → 起票が記録に残らない。商品案名も概要も無い案件ができる
- **G6（発売後評価）を外す** → 機種追加の売上が評価されない

**なぜこの切り方か**:
- **G0 は通す。**起票はすべての案件の入口で、F-6 の表でも「起票は軽いままにする」
  とあるだけで、④を除外していない
- **G1（評価通過）は対象外。**既存商品の機種追加なので、採点・重複チェックの対象ではない
- **G3（コンセプト承認）は対象外。**現行フローにも**④には「社長の確認」が存在しない**
  （要件定義 F-6 の前書きで実査済み）
- **G4（生産可否確定）は対象外。**生産方法は本体商品で確定済み
- **G6 は通す。**これは要件定義 F-6-4 の字面を**広げた**判断。売上の評価を外す理由が無い。
  もし「④は発売後評価も不要」であれば、`applies_to_flow_types` から外すだけで直る

**`対象外` は「未達」ではない。**空欄のままだと永久に未達に見えるため、
状態として `対象外`（記号 `/`）を持たせ、画面に「放置ではなく設計です」と出している。

**影響**: FR-54
**根拠**: `app/seed.py` の `NOT_NEWMODEL` ／ `app/gate.py` の `applies()` ／ 設計書 §5-6

---

## ADR-012 — タスクの期限を自動割付しない（2026-09-21）

**決定**: テンプレート展開時に**開始日・期限を入れない。**期限は人が入れる。

**要件は割付を求めている。**F-5-2 は「営業日マスタを使って開始日・期限を自動割付」。
**これを第2段では実装しない**と決めた。

**なぜ**:
1. **営業日マスタは calfc が正本**で、その連携（設計書 第11章 ⑧ `service_tokens.json` への
   追加）が**まだ依頼できていない**。本アプリ側に営業日を持つと N-12（マスタは1箇所）を破る
2. **推測で日付を入れると、実運用の「期限空欄88件」が見えなくなる。**
   空欄は問題の在処であって、埋めれば消える種類の問題ではない。
   自動で入れた日付は「誰も決めていない期限」で、守られない

**いま起きること**: 案件を作るとタスクは全件「期限なし」で並ぶ。
タスク画面には**「期限なし」を常設ボタン**として置き、件数が見えるようにしてある。

**影響**: FR-41（未着手のまま残す）・FR-45
**根拠**: `app/project.py` の `expand_tasks()` ／ `tests/e2e_http.py`「期限なしで32件」

---

## ADR-013 — 旧テンプレート（190行）を採用しない（2026-09-21）

**決定**: フローテンプレートの正本は
`03_商品開発フローAI効率化分析__タスク別AI適用一覧.tsv` の **178行**。
`01_進捗管理商品開発__テンプレート.tsv`（タスク190行）は**採用しない。差分は記録のみ。**

**なぜ**: 2つの表は既に乖離している（要件定義 2-1 が「178 ／ 196 ← **既に乖離**」と実査）。
`03` 側は担当ロール・標準工数・AI適用カテゴリ・削減率を持ち、
**本アプリが要る列がすべて揃っている。**`01` は古い。

**ただし「予備時間」の行は `01` にしか無い。**F-5-7 が明示的に「持て」と言っている項目なので、
**値だけ `seed.old_reserve_hours()` に記録してある**（`task_template` には入れていない）。
採否は商品開発部の判断（HANDOFF の宿題）。

**「196行」という数え方について**: 各フローの「商品名」見出し行6件を含む数え方。
タスクの行は190件。**件数を引用するときはどちらの数え方かを添える。**

**影響**: FR-39・FR-46・FR-154
**根拠**: `app/seed.py` の `diff_report()` ／ 要件定義 2-1

---

## ADR-014 — `/opt/newproduct` を 750 にした（2026-09-21）

**決定**: `/opt/newproduct` の所有者は `newproduct:newproduct`、**パーミッションは 750**。
`config/` `logs/` `migrations/` `seed/` `tests/` `app/` も 750（`drwxr-x---`）。

**なぜ 755 ではないか**:
- `config/` には `users.json`（scrypt ハッシュ）・`.sessions.json`・`svc_token` が入る。
  **同じサーバー上の他アプリのユーザー**（lpscope・autogrowth・calfc・seisan・keiei）から
  読めてよいものではない
- 6アプリは `funcreate-auth` グループを共有している（FR-04）。
  **グループが同じということは、755 にすると全アプリから読める**ということ。
  共通ルール §4 の「グループに w を足さない」と同じ理由で、r も絞る
- `newproduct` ユーザー自身は問題なく読める。systemd の `User=newproduct` で走る

**副作用（把握している）**: root 以外から `ls /opt/newproduct/app/*.py` のような
glob が通らない。調査時は `sudo sh -c "..."` で囲む必要がある。
**これは不便であって欠陥ではない**ので、受け入れる。

**影響**: FR-14・FR-24
**根拠**: 設計書 第11章 ① ／ 2026-09-21 `ls -la` で実測（uid 988・750）

---

## ADR-015 — 未実装のサーバ間APIは 501 を返す（2026-09-21）

**決定**: `/api/svc/plan` `/api/svc/themes` `/api/svc/ingest` は、実装前の段階で
**404 ではなく 501** を返す（`contract_version` を添えて）。

**なぜ**: 404 にすると、呼ぶ側が**「経路が無い（綴り間違い）」と「まだ無い（未実装）」を
区別できない。**相手セッション（AutoGrowth・keiei・calfc）が連携を試したときに、
自分の側の間違いを疑って時間を使うことになる。

**影響**: FR-27
**根拠**: `server.svc()` ／ 設計書 §6

---

## ADR-016 — 分からないものを 0 で埋めない（2026-09-21）

**決定**: 次の2つは **NULL のまま**入れ、画面に「未確定」と出す。

| 対象 | なぜ 0 にしないか |
|---|---|
| `flow_type.effort_point` の ⑤資材リニューアル | 2026年度の年間プランに1件も使われておらず**実測できない**。0 で埋めると「工数ゼロの安いフロー」に見え、**逆に危ない** |
| ⑦ページリニューアルの標準タスク | 178行に1行も無い。**発明しない。**計画の38%を占めながら定義が無いこと自体を画面に出す |

**なぜ**: 共通ルール §7「**根拠のない数値を出さない**」「**『無い』と『0』を混同しない**」。
埋めた瞬間に、**それが推測だったことが誰にも分からなくなる。**

**影響**: FR-21・FR-49・FR-87
**根拠**: `app/seed.py` の `FLOW_TYPES` `FLOW_NOTE` ／ `selfcheck.py`（係数未確定が1つ、を検査している）

---

## ADR-017 — 移行したアイデアは 881 件（「1,505件」ではない）（2026-09-21）

**決定**: 4シートから **881件**を `idea` に移した。内訳は
オリジナル **419** ／ 推し活うちわ **413** ／ ぶっこみ **2** ／ LOVOT **47**
（うち LOVOT の5件は末尾の【追加】欄で、採点されていない）。
v1 のスコアが付いたのは **874件**。

**なぜこの数か**: 要件定義・全体設計書は「オリジナル750件・推し活うちわ755件・
合計1,505件」と書いている。**書き出された TSV で商品案名が入っている行は
419行と413行**で、残りは書式だけが残った空行だった
（通年性の分布を「419件中365件が8点固定」と書いている箇所とは一致する）。

**取り込むのは商品案名のある行だけ。**空行を件数に数えない。
**「1,505件あるはず」に合わせて水増ししない。**

**影響**: FR-65・FR-157
**根拠**: `tools/import_ideas.py` の出力 ／ `tests/test_ideas.py` の `TestImport`

---

## ADR-018 — 想定粗利額の「円→点」の刻みは暫定（2026-09-21）

**決定**: F-1-7 に従い「高単価化しやすさ(1〜10点)」を**円建ての想定粗利額**に
置き換えた。円を1〜10点に写す刻みは `rubric.margin_bands` に**データとして**持ち、
画面に「**暫定です**」と出す。

**なぜ暫定と言うか**: 刻みの根拠になる原価の正本（seisan の材料原価）は
**第3段で入る。**いまは実測できない。**根拠が無いまま確定値の顔をさせない**
（N-10・共通ルール §7）。データで持つので、確認が取れたら差し替えるだけで済む。

**残っている確認**: 商品開発部に「1個あたり想定粗利額の実感値の分布」を聞く。

**影響**: FR-70
**根拠**: `app/idea.py` の `MARGIN_BANDS` `MARGIN_BANDS_NOTE`

---

## ADR-019 — 「6か月を割ったら赤」を語で出す（2026-09-21）

**決定**: F-1-14 は「コンセプト在庫月数が6か月を割ったら**赤で出す**」と書いているが、
**色ではなく「不足／充足」の語**で出す。

**なぜ**: N-11「**色に意味を持たせない**」。色だけで表すと、色覚・白黒印刷・
ダークテーマのいずれかで意味が消える。この規律はゲートの状態（`gate.STATES`）でも
同じ扱いにしてある。**要件の意図（下回ったら気づける）は語で満たせる。**

**影響**: FR-77
**根拠**: `app/idea.py` の `concept_stock()` ／ `selfcheck.py`（ランクを色で表していないことを検査）

---

## ADR-020 — 機会カレンダー（ライフイベント採点）をこの段では取り込まない（2026-09-21）

**決定**: `theme` `theme_score` `theme_signal` の表は作ったが、
**ライフイベントの暦と採点は 0 件のまま**にした。入れたのは
採点に要る「評価テーマ」4件（ライフイベント／推し活／LOVOT／ぶっこみ）だけ。

**なぜ**: 元表 `01_進捗管理商品開発__イベント.tsv` は、**行によってライフイベントの
列位置が 8列目と10列目に混在している。**機械的に読むと静かに取り違える。
総合スコアの式（購買意欲×2＋写真親和性×1＋発生頻度×2＝満点40）は実測で確定しているので、
**式が分からないのではなく、どの行がどのイベントかが分からない。**
半分だけ読んで入れるより、**0件のまま「未取込」と言うほうがよい**（共通ルール §7）。

**次にやること**: F-2（機会カレンダー）で、列位置のずれを人が確認しながら取り込む。

**影響**: FR-78・FR-79・FR-158
**根拠**: `migrations/006_theme.sql` の注記 ／ `selfcheck.py`（0件であることを検査）

---

## ADR-021 — 長期連休の月は「未計測」と返す。推測で埋めない（2026-09-22）

**決定**: 挿入ルール4本のうち「長期連休のある月は多くしない」（F-3-2 ③）は、
設定 `plan.holiday_months` と `plan.holiday_month_max_slots` を**空のまま出荷**し、
検査結果に `unavailable`（未計測）を返す。**`ok` に混ぜない。**

**なぜ**: どの月が長期連休かの正本は **Calendar（calfc）の会社休業日**で、まだ連携していない。
「1・5月・8月」と置くのは常識の範囲に見えるが、**置いた瞬間にそれが根拠になる。**
上限の本数に至っては出典に数字が無く、「多くしない」としか書かれていない。

この日の作業で繰り返し出た形と同じ。**落ちるのではなく、黙って既定値に落ちるのが危ない。**
`unavailable` なら画面に「未計測 1件」と出て、人が見る。`ok` なら誰も見ない。

**影響**: FR-83（`実装済` にせず `実装中` に留めた）
**根拠**: `app/plan.py` `_rule_holiday()` ／ `tests/test_plan.py`
`test_holiday_rule_reports_unavailable_instead_of_passing` ／ e2e「数えられない連休ルールは『未計測』」

---

## ADR-022 — 3:1 の「括り」をコードに焼かず、`product_kind.ratio_group` に置く（2026-09-22）

**決定**: 挿入ルールの比率 3:1 は、商品タイプ表の `ratio_group` 列
（`original` / `uchiwa` / NULL）を数えて判定する。比の数だけを設定
`plan.ratio_original_to_uchiwa` に持つ。**どのタイプがどちら側かはデータ。**

**なぜ**: 出典（2026年度 年間プラン）の表記は「**オリジナル（推し活）：うちわ**」。
一方このアプリの評価テーマは「**ライフイベント（オリジナルグッズ）／推し活（うちわ）**」で、
**括弧の中が逆**。どちらの括りが正かは商品開発部にしか決められない。
コードに `if kind == "original"` と書くと、直すのにデプロイが要る。

種データは `ratio_group` を**上書きしない**（`seed()` が更新する列から外してある）。
商品開発部が画面や SQL で付け替えたものを、次の seed で戻さないため。

**許容差は枠1本**（`plan.ratio_tolerance_slots`）。年間26枠で 3:1 は割り切れず、
端数のたびに警告が出ると誰も読まなくなる。

**未決**: どちらの括りが正か。**商品開発部への確認事項**（FR-87 と同じ束で聞く）
**影響**: FR-83
**根拠**: `migrations/009_plan.sql` の `product_kind` 注記 ／
`tests/test_plan.py` `test_seed_does_not_overwrite_a_hand_changed_ratio_group`

---

## ADR-023 — 枠の工数ポイントと、標準タスクの時間(h)を足さない（2026-09-22）

**決定**: 月間 10〜15 の検査は `flow_type.effort_point`（フロー係数。名入れ 5.5・
フリーカット 6.0・ページリニューアル 2.5）の月合計で行う。
**`task_template.standard_hours` の合計とは別物**として扱い、画面にもそう書く。

**なぜ**: 単位が違う。片方は「フローの重さ」、もう片方は「実作業の時間」。
さらに `standard_hours` は**予備時間を含まない**（178行の標準タスクに予備時間の行が
1つも無い）。混ぜると、予備時間の扱いが決まった日に**両方の数字が同時に動く。**

**予備時間の扱いが未決であることは、この F-3 の実装を止めない。**
時間側の月次負荷（`task.dashboard()`）だけが影響を受ける。

**あわせて決めたこと**: 工数ポイントが未確定（⑤資材リニューアル＝係数 NULL）の枠は、
**0 として合計に足さない。**別に件数を数えて「未確定 N 本を含みません。
実際はこれより重くなります」と出し、その月の判定を `ok` にしない。
0 で足すと「軽い月」に見えて、さらに枠を足してしまう。

**人待ち**: 予備時間を `task_template` に入れるか（十文字さん・2026-09-21 から）
**影響**: FR-83・FR-45
**根拠**: `app/plan.py` の冒頭注記と `check()` の `effort_unit_note` ／
`tests/test_plan.py` `test_unknown_effort_points_are_not_counted_as_zero`

---

## ADR-024 — 商品タイプ未設定の枠は、発売本数に「数える」（2026-09-22）

**決定**: 「月3商品」の本数は `product_kind.counts_as_launch` で決める。
ページリニューアルは **0（数えない）**。**商品タイプが未設定（NULL）の枠は 1 として数える。**

**なぜ**: ページリニューアルを数えないのは F-10-11（計画の38%を占めるため、
混ぜると「月3商品」が達成に見える）。
一方で、**タイプを選び忘れた枠まで数から外すと、選び忘れが静かに消える。**
SQL の LEFT JOIN で NULL になるだけの行を「数えない」に倒すと、
**入力漏れが「達成」に化ける。**明示的に 0 と書かれたときだけ除く。

**影響**: FR-83
**根拠**: `app/plan.py` `_counts_as_launch()` ／ `tests/test_plan.py`
`test_pagerenew_is_not_counted_but_a_missing_kind_is`

---

## ADR-025 — 日が未定の枠は、その月の1日で逆算する（2026-09-22）

**決定**: 枠→案件の変換（FR-86）で返す「タスクを組み終える期限」は、
発売予定日の2か月前。**発売日が未定の枠は、その月の1日を発売日とみなして逆算する。**
案件側の `launch_date` には**仮の日付を入れない**（NULL のまま）。

**なぜ**: 月末で計算すると期限が最大30日うしろへずれる。**早いほうに倒すのが安全側。**
案件に仮の日付を入れてしまうと、そこから先のすべての期限がその仮日付で動き出し、
あとで本当の日が決まったときに、どれが仮だったのか分からなくなる。

**影響**: FR-82・FR-86
**根拠**: `app/plan.py` `task_setup_due()` ／ `tests/test_plan.py`
`test_a_month_only_slot_counts_back_from_the_first_of_the_month`

---

## ADR-026 — Caddy が `/api/*` を全部落としていた（2026-09-22・障害）

**何が起きていたか**: `/etc/caddy/Caddyfile` の newproduct ブロックが
`@svc path /api/*` → `respond 404`。このアプリは画面が SPA で、
ダッシュボード・案件・タスク・アイデア・プランの**すべてが `/api/...` を叩く。**
つまり **ログインしたあとの画面が全部「表示できませんでした」**だった。
ログイン画面だけはサーバー側生成なので、正常に見えていた。

**なぜ気づかなかったか**: `selfcheck.py`（118件）も `tests/e2e_http.py` も
**127.0.0.1 しか見ない。**Caddy の段は通らない。ループバックでは全部 200。
**「HTTP は全部通っているのに画面が壊れている」**という、
共通ルール `rules/eod.md` が挙げているのと同じ形（2026-09-20 AI-Assistant）。

**原因**: keiei（business-analysis）の断片を写したこと。
**あちらは画面がサーバー側生成で、`/api/*` は本当にサーバ間専用**なので
`path /api/*` で正しい。こちらは SPA。**同じに見える断片の、前提が違った。**

**直したこと**:
- matcher を `/api/svc/* /api/health` に変更（`/etc` と `deploy/` の両方）
- `/api/health` は外に出さない。件数（アイデア881件・利用者数・ポート）を返すため。
  死活は `/healthz`（"ok" の1語）
- `selfcheck.py` に断片の matcher を検査する2件を追加（**わざと壊して落ちることを確認済み**）
- 手順書に「画面を足したら本番のURLで1回叩く」を追加

**残る穴**: selfcheck が見られるのは `deploy/` の複製だけで、**`/etc` の正本は読めない**
（`newproduct` ユーザーで動いているため）。**外から1回叩く手順**で埋めている。

**他アプリへの確認**: keiei は画面がサーバー側生成のため現状のままで正しい
（`server.py` の `/api/*` は kpi と ingest のみ）。lpscope 401・autogrowth 403 で
別方式。**このブロックの取り違えは newproduct だけ。**

**影響**: FR-01〜FR-86 の画面すべて（要件IDとしては横断）
**根拠**: `curl https://newproduct.fun-create.co.jp/api/plan` が 404 → 401 になったこと ／
`deploy/Caddyfile.fragment` ／ `selfcheck.py` の Caddy 断片2件

---

## ADR-027 — 「今月の枠の消化」は承認済み版だけで数える。0本は未計測ではない（2026-09-22）

**決定**: ダッシュボード2段目の「今月の枠の消化」（FR-63）は、
**承認済みの年間プラン**の今月の枠のうち、開発案件に変換した数で出す。
状態は3つに分ける — `未計測`（承認済み版が無い）／`枠なし`（**今月は枠を置いていない**）／
`消化済` `未消化あり`。

**なぜ承認済みだけか**: 策定中の版を分母にすると、**下書きに枠を足すたびに達成率が下がる。**
問いは「期首に約束したものを消化できているか」であって、下書きの進み具合ではない。

**なぜ「枠なし」を分けるか**: 0本は**事実**であって欠測ではない。
未計測に混ぜると「まだ実装していないのか」という催促が止まらず、
逆に 0 と書くと「消化できていない」と読まれる。**どちらでもない語を用意した。**

**あわせて**: 画面に「対象: <版の名前> の <月>」を出す。
**どの版の何月を見た数字なのかが書かれていないと、確かめようがない。**

**影響**: FR-63（`実装中` のまま。3つのうち「発売後チェックの未処理」が第4段のデータ待ち）
**根拠**: `app/plan.py` `slot_consumption()` ／ `tests/test_plan.py` `TestSlotConsumption` 5件

---

## ADR-028 — 予備時間は旧表の半分・1人分。実作業と合算しない（2026-09-23）

**決定: 十文字さん。**言葉:「予備時間が多すぎるので、現在の半分の時間にして1人分にしてほしい。
例16時間×2人であれば、8時間だけ」／選択肢C「足すが、実作業と予備を分けて出す」。

**量**: 旧テンプレートは1フローにつき **管理者・メンバーへ同額**を置いていた
（名入れなら各16.0h・計32.0h）。これは**実作業の合計（17.0h）とほぼ同じ大きさ**で、
元のAI削減分析も「各フローの大きなバッファ」として集計から外している。
採用値は**合計をその半分**にしたもの。

| フロー | 実作業 | 旧・予備（合計） | 採用 |
|---|---|---|---|
| 名入れ商品 | 17.000h | 32.00h | **8.000h** |
| フリーカット商品 | 20.750h | 39.50h | **9.875h** |
| Webdeco商品 | 19.750h | 37.50h | **9.375h** |
| ニューモデル追加 | 6.750h | 13.50h | **3.375h** |
| 資材リニューアル | 17.750h | 35.00h | **8.750h** |
| 既成デザイン商品 | 11.250h | 22.50h | **5.625h** |

**端数を丸めない**（19.75 ÷ 2 = 9.875）。丸めると「半分」でなくなり、元の値へ戻せない。

**担当**: 合計を 管理者・メンバーで**等分**した（名入れなら 4.0h ＋ 4.0h）。
**片方へ寄せない。**試しに全部を管理者へ置いたところ、管理者だけ 43.25h → 88.25h となり、
**6フロー合算のボトルネック（管理者 43.25h ／ メンバー 44.50h）が逆転した。**
旧表に無い偏りを、こちらで作らない。寄せ方を変えるときは `seed.RESERVE_SPLIT` を直す。

**合算しない**: 行に `kind`（`実作業` / `予備`）を持たせ、月次負荷・テンプレート合計・
`/api/health` のいずれでも**別の数**として出す。合算すると、AI削減の試算（予備を除外して
作られている）と月次が同じ数字を指さなくなる。**予備の行に AI削減率は入れない。**

⑦ページリニューアルには入れない（旧表に予備時間の行が無い。**発明しない**）。

**影響**: FR-46（未着手→実装済）・FR-45
**根拠**: `migrations/010_reserve.sql` ／ `app/seed.py` `seed_reserve()` ／
`tests/test_second_stage.py` `test_reserve_hours_are_half_of_the_old_table`

---

## ADR-029 — 想定粗利額の刻みは、このまま採点を始める（2026-09-23）

**決定: 十文字さん**（選択肢A）。暫定の境界（4,000円以上＝10点 … 0円＝1点）のまま、
v2 の採点を始める。

**なぜ**: 881件が v1 の点のままだと**テーマをまたいで横並びができない。**
原価の正本（seisan の材料原価）は第3段まで入らず、待つと第3段まで v2 が0件のままになる。

**あとで刻みを変えるときは、新しい版（v3）として足す。**いまの点は書き換えない
（F-1-10。版は合算しない）。`rubric.margin_bands` が版ごとに刻みを持っているので、
差し替えても過去の点は壊れない。

**影響**: FR-70。ADR-018（刻みは暫定）をこれで確定扱いにする
**根拠**: `app/idea.py` `MARGIN_BANDS` ／ `MARGIN_BANDS_NOTE`

---

## ADR-030 — 3:1 は「うちわ か、そうでないか」で数える（2026-09-23）

**決定: 十文字さん**（選択肢A「うちわ以外を3、うちわを1として数える」）。

**なぜ決めが要ったか**: 出典（2026年度 年間プラン）の表記は
「**オリジナル（推し活）：うちわ**」。一方このアプリの評価テーマは
「**ライフイベント（オリジナルグッズ）／推し活（うちわ）**」で、**括弧の中が逆**だった。
ADR-022 でコードに焼かずデータに置いてあったので、**設定ではなく数え方の決定**で済んだ。

**実装**: 「1」の側は `ratio_group='uchiwa'` だけ。「3」の側は**発売本数に数える枠のうち
うちわ以外すべて**。したがって**商品タイプの付け忘れも「うちわ以外」として数える**
（ADR-024 と同じ考え。外すと入力漏れが静かに消える）。
比から外すのは発売本数に数えないもの（ページリニューアル）だけ。

**ADR-022 からの変更点**: `product_kind.ratio_group` が `original` でない枠
（その他・未設定）を、以前は**比率の対象外**にしていた。これを3の側へ入れた。

**影響**: FR-83
**根拠**: `app/plan.py` `_rule_ratio()` ／ `tests/test_plan.py`
`test_slots_without_a_kind_count_as_not_uchiwa` ・ `test_other_kinds_are_on_the_non_uchiwa_side`

---

## ADR-031 — マイグレーションは未適用のものだけ流す（2026-09-23）

**決定**: `store.migrate()` が `schema_migration` を見て、**まだ流していないファイルだけ**流す。

**なぜ**: それまで毎回すべて流し直していた。`CREATE TABLE IF NOT EXISTS` はそれで平気だが、
**`ALTER TABLE ADD COLUMN` には `IF NOT EXISTS` が無い。**010 を足した瞬間、
**2回目の起動が `duplicate column name` で落ちる**ところだった。
`server.py` は起動のたびに `seed.run()` →`migrate()` を呼ぶので、**本番が上がらない。**

テストが先に捕まえた（`--dry-run` の件と同じく、**落ちない失敗ではなく落ちる失敗**だったのが幸い）。
検査 `test_migrate_runs_each_file_once` は**壊してから1度動かして、落ちることを確かめてある。**

**副作用**: **流したあとに中身を書き換えても、もう流れない。**直すときは新しい番号を足す。

**影響**: 全要件（起動そのもの）
**根拠**: `app/store.py` `migrate()` ／ `tests/test_second_stage.py`
`test_migrate_runs_each_file_once`

---

## ADR-032 — 機会カレンダーは「式で裏を取って」取り込む。ADR-020 を取り消す（2026-09-23）

**決定**: 2026-09-21 に「列位置がずれていて機械で読むと静かに取り違える」として
0件のままにした表（ADR-020）を、**読み方を変えて取り込んだ。**

**何が変わったか**: 09-21 は**列位置で読もうとして**いた。今回は**列位置を使わない。**
数値5連を見つけ、**総合 ＝ 購買意欲×2 ＋ 写真親和性 ＋ 発生頻度×2（満点40）**で
検算し、**合ったものだけ**を採点行とみなす。19件すべて誤差0。
2つ目の裏取りとして、総合スコアが 38→34→32→29×5→28→27×4→26×3→25×3 と
単調に下がり、優先順位も 1→8 と切れ目なく続くことを確認した。
**ずれた7件は、同じ並びの続きだった。**

**09-21 の恐れは正しかった**: 10列目には**別のもの**が入っている。行25以降の
「新商品が作れていないライフイベント」13件（商品例つき）が同じ列を使う。
**列位置だけで読んでいたら、この2つが混ざっていた。**恐れが間違っていたのではなく、
**読み方の当てが外れていた**（`rules/apps-common.md` §7 の「道具の都合で見えていない」）。

**入れたもの**: 年間イベント53件（FR-78）／採点済みライフイベント19件（FR-79）／
新商品が作れていないライフイベント13件。

**入れなかったもの**:
- 元表59〜185行目（年齢別ライフイベント × 購入者の◯印・124行）。**形がまったく違う別の資産**で、
  F-2 の要件ではない。入れないことを `tools/import_events.py` の冒頭に書いて残した
- 元表244〜249行目（絵文字つきの走り書き・12月の商戦メモ）
- **日付の `MM-DD` 化。**「11月15日前後の土日」「1月の第２月曜日」「7〜8月」のように
  直せない行が多い。**直せた行だけ直すと、日付が入っている行が実際より正確に見える**

**推測しなかったもの（人待ち）**: 6列目「販売可能性が高い年間イベント」12件のうち
**6件が年間イベント名と文字一致しない**（「入園入学」と「入学式」、
「部活動引退（夏～秋）」と「部活動引退」など）。人が見れば同じだが、
**「七五三/犬の日」は2つのイベント**で、機械で寄せると壊れる。
**印を付けずに、確認用の一覧へ出した**（`tools/import_events.py --review`）。

**残したもの**: `theme.source_row` / `source_col`（1始まり）。
**どの行をどちらとして読んだか**が残っていないと、あとから人が判断の是非を検められない。

**影響**: FR-78・FR-79・FR-158（いずれも未着手/実装中→実装済）
**根拠**: `tools/import_events.py` ／ `tests/test_events.py` 14件 ／
`migrations/011_opportunity.sql`

---

## ADR-033 — 自動化依頼を受け付ける（F-15）。ただし**実装はしない**（2026-09-24）

**決定: 十文字さん**（商品開発 増地さんの要望）。
「自動化実行依頼案として要望をリスト化する機能」と、
「案件を投げた人に質問をしながら要件定義が完了したら、機能を実装するというUI」。

**このアプリは実装しない、と決めた。**受け付け → 質問 → 要件の書き出し →
**作る人へ渡す**までが役目で、画面にもそう書いた。アプリの中で実装まで走らせる形は、
①何を実装するかが依頼ごとに違いすぎる ②AI予算枠が未取得（FR-146）③失敗したときに
誰も気づけない、の3点で持てない。**「要件が揃う前に渡さない」ことのほうが効く。**

**芯は1つ**: `set_stage()` が、必須8問の答えが揃うまで `要件確定` 以降を断る（FR-163）。
揃わないまま渡すと、作る側が想像で埋める。

**質問は固定の8問**（FR-161）。AI に作らせない（予算枠が未取得）。
8問はどれも「答えが無いと実装できない」ものに絞った。
とくに **「人が見ないと決められないところはどこか」**が、自動化の可否を分ける。
**質問ごとに「なぜ聞くか」を画面に出す。**理由の無い質問は、埋めるだけの欄になる。

**未回答と「無いという答え」を分ける**（FR-162・N-10）。行が無い＝未回答、
`answer=''`＝聞いた結果「無い」。**空欄を『無し』と読み替えない。**

**`work_item` に入れなかった理由**: `work_item` は**やる仕事**、こちらは
**やらなくて済ませたい仕事**で、終わり方が違う。work_item は「完了」で閉じるが、
自動化依頼は**「実装されて、以後その作業が発生しなくなる」**ことで閉じる。
同じ表に入れると月次の工数に二重に載る。

### 実測で分かったこと ── **15件中11件が標準タスク178行に無い**

| 当たる4件 | 178行側 |
|---|---|
| 新規商品のシミュレーションデータ作成 | Webdecoシミュレーション提出用データを作成（4フロー・AI削減40%） |
| 新商品発売時の作成方法検討 | 作成方法を検討 ／ データ作成方法を検討 |
| 作成用テンプレート作成 | データ作成用テンプレートを作成（70%） |
| LP依頼シート作成 | 商品ページ依頼準備（LP依頼シート）（4フロー・70%・1.40h） |

**最初に「5件」と数えたのは誤り。**「スタンプ登録」を
「この商品用のスタンプ・**背景を検討**」に当てていたが、**検討と登録は別の作業**。
「シミュレーションの修正」も、178行には 作成・提出・確認・共有はあるが**修正が無い**。

**11件は「やっていない」のではない。**増地さんが毎日やっている作業が、
標準タスク表に載っていないというだけ。**表のほうが現場に追いついていない。**
`in_template=0` で数えられるようにし、画面にもその読み方を書いた（FR-166）。
昇格させるかどうかは商品開発部の判断なので、FR-168 として未着手で置いた。

**帯（NAV）には足さなかった。**7つが上限（keiei の `layout.py`・selfcheck が見張っている）。
`ALIAS` で「タスク」の下に置き、タスク画面から入口を張った。

**影響**: FR-159〜FR-170（新設12件。実装済9・未着手2・見送り1）
**根拠**: `migrations/012_automation.sql` ／ `app/automation.py` ／
`tools/import_automation.py` ／ `tests/test_automation.py` 17件 ／ e2e 8件

---

## ADR-034 — ChatWork へ渡す。ただし**自動送信はしない**（2026-09-24）

**決定: 十文字さん**（選択肢C「ChatWork へ自動で送る」）。

**選択どおりに作らなかった点が1つあります。**「要件が確定したら自動投稿」ではなく、
**送る文面をそのまま見せて、人が押したときだけ送る**形にした。理由は2つ。

1. **ChatWork は取り消せない。**誤った部屋・誤った内容は消せない
2. **lpscope が同じ ChatWork に対して「対外送信＝人の明示操作が必須。自動送信しない」を
   規約にしている**（`/opt/lpscope/reports/chatwork.py` の冒頭）。
   **同じ部屋へ投げる以上、こちらだけ黙って自動送信しない。**

**完全自動にもできます。**必要なら `set_stage()` から `chatwork_send()` を呼ぶだけ。
十文字さんの一言で切り替えます。

### トークンを写さない

ChatWork のトークンは Auto GROWTH・secretary・lpscope が既に持っているが、
いずれも 0600 で別ユーザー。**newproduct からは読めない（実測）。読めないのが正しい。**

`config/chatwork.env` に**このアプリ専用のトークン**を置いてもらう（0600・git管理外）。
**他アプリのものを写さない。**写すと ①どのアプリが投げたか分からなくなる
②片方を止めると両方止まる。**無ければ送らず、何が足りないかを画面に出す**（N-10）。

`.gitignore` に `config/chatwork.env` と `config/*.env` を追加。
selfcheck が「トークンを直書きしていない」「gitignore にある」「あるなら600」を見る。

### 二度押しで二重投稿しない

`sent_count` / `sent_at` / `sent_room` / `sent_message_id` を持ち、
2回目は断る。**再送は明示的に選んだときだけ。**取り消せない以上、
「押したかどうか分からない」状態を作らない。

### 本文は ChatWork 記法

**Markdown は効かない**（`**強調**` はアスタリスクがそのまま出る）。`[info][title]` で組む。
**名乗らない**（自分のアカウントから出る）。
**未回答は「未回答」と書く。**渡された側が穴に気づけないと、渡す意味がない。

**影響**: FR-171〜FR-175（新設5件。実装済4・未着手1）
**根拠**: `app/chatwork.py` ／ `app/automation.py` の `chatwork_*` ／
`migrations/013_handoff.sql` ／ `tests/test_automation.py` `TestChatWorkHandoff` 9件

---

## ADR-035 — 「渡す」が決まっていなかったことの記録（2026-09-24）

**十文字さんの質問**:「作る人へ渡すは誰にどのように渡すのですか？」

**そのとき実装されていたもの**: `handoff_to` という**自由記入の1行**と、
要件を Markdown で開くページだけ。**候補リストも通知も無く、誰にも届かなかった。**
F-15 を作ったとき、そこを詰めずに「渡す」と書いていた。

**あわせて分かったこと**: 渡し先は1つではない。15件のうち、
- **AI で作るもの**（画像生成・文書生成）が約7件 → **AI予算枠が未取得**（FR-146）
- **WebDeco の画面を触るもの**（背景・スタンプ・カテゴリ）が3件 → **経路が無い**
- seisan のデータ集計が1件 → 作れる
- 治具作成 → **自動化の対象かどうかから怪しい**

**渡し先を決める前に、この2つ（AI予算枠・WebDeco の経路）が要る。**
FR-168（標準タスクへの昇格）と同じく、人の判断待ちとして残す。

---

## ADR-036 — AI予算の通し方は server-to-server。**記録漏れの反省つき**（2026-09-24）

**枠は入った**: Auto GROWTH が `per_job` に `newproduct-*: 5.0` を追加（全体50.0据え置き・合計20.0）。

**通し方は ② server-to-server**（Auto GROWTH の実測による決定）:
`/opt/autogrowth` には newproduct へ `--x` があるが、**`/opt/autogrowth/config` が
`rwx------` で親から通り抜けられない。**`ai_budget.json` 自体の権限が緩くても届かない。
ACL を2か所開ける案（①）は採らず、日次受け渡し（③）は上限が後追いになるため不採用。

口は `POST http://127.0.0.1:8789/api/ai-usage`。Auto GROWTH が作る。
**できるまで着手しない**（枠だけ入れても、記録が届かなければ上限は効かない）。

**当方の既定**: **口につながらないときは AI を使わない。**`unavailable` として止め、
画面に理由を出す。「予算を確かめられないので実行しない」。黙って使うほうが危ない。

### 反省 ── 承認を `decisions/` に記録していなかった

十文字さんから直接いただいた承認（予備時間・粗利額の刻み・3:1・OD-5・ChatWork・AI予算）を、
**このファイル（app 側の `DECISIONS.md`）にだけ書き、HUB `decisions/2026-09.md` に置いていなかった。**

`rules/orchestrator.md` 62〜68行が「**十文字さんの指示は記録で伝わる。名乗りでは伝わらない**」
「受け取った側は、メッセージではなく記録を自分で読んで確かめる」と定めている。
そのため AI予算枠の依頼を受けた Auto GROWTH は「伝言だけでは支出を動かせない」として
**十文字さんに直接確認する手間をかけた**（2026-09-24 23:43）。

**承認を取った側が記録まで置く。**2026-09-24 に本日ぶん7件を
`decisions/2026-09.md` へ記録した（承認の言葉つき・(A) 直接の指示として）。

**影響**: FR-146（未着手→実装中）
**根拠**: `hub cat decisions/2026-09.md`（2026-09-24 の2節）

---

## ADR-037 — AI予算は分けず 5.0 を共有する。契約の形が確定（2026-09-24）

**決定**: `newproduct-image` と `newproduct-text` で `newproduct-*: 5.0` を**分け合う**。
`newproduct-image: 3.0` / `newproduct-text: 2.0` のように分けることもできるが、**しない。**

**なぜ**: **分ける根拠になる実績が1件も無い。**`ai_scored: 0`、自動化依頼15件は全件が起票のまま。
画像が文書より高いのは一般論として分かるが、**この15件でどちらが何割になるかは測っていない。**
いま 3.0/2.0 と置けば、それは推測値であって、**置いた瞬間に根拠に見える**（N-10）。

**片方が使い切るともう片方も止まる**が、**黙っては止まらない**。Auto GROWTH 側が
`over_cap` で断り、`remaining` を返すので、画面に「予算を使い切りました（残 0.0）」と数で出せる。
**分けるのは1か月ぶんの実績が出てから。**

### 罠 ── `newproduct`（ハイフン無し）は枠に入らない

`cap_rule("newproduct")` は `(0.0, '')` を返し、**job 別の上限が効かず、全体 50.0 だけが効く。**
job 名を1文字間違えると、**このアプリが会社全体の枠を引ける。**

**呼び出す側で、送る前に弾く。**`newproduct-` で始まらない job 名は例外にする
（実装は口ができてから。**忘れないようここに書いておく**）。

### 契約（Auto GROWTH が作る）

    POST http://127.0.0.1:8789/api/ai-usage
      {"job":"newproduct-image","model":"...","usd":0.42,"request_id":"...","note":"..."}

    {"ok":true,"job":"newproduct-image","cap":5.0,"spent":0.42,"remaining":4.58,"counted":true}
    {"ok":true,...,"counted":false}                  ← request_id が既出（再送・合計は増えない）
    {"ok":false,"reason":"over_cap","cap":5.0,"spent":5.0,"remaining":0.0}

- **超過は記録の前に断る**（断った呼び出しは使っていないので記録しない）
- **`request_id` で冪等。**再送しても合計が増えない
- **`remaining` を返す。**こちらで引き算しない（上限を変えた瞬間にずれるため）

**当方の既定**: **口につながらないときは AI を使わない。**`unavailable` として止め、
画面に理由を出す。「予算を確かめられないので実行しない」。

**着手**: 口ができてから。**枠だけ入れても、記録が届かなければ上限は効かない。**

**影響**: FR-146
**根拠**: Auto GROWTH の実測（`cap_rule` / `month_spend_scope` の前方一致）
