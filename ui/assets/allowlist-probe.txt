このファイルは検査用です。

/assets/ の配信は**拡張子の許可リスト方式**（server.py の ASSET_TYPES）で、
リストに無い拡張子は「存在していても配らない」。
それを確かめるために、**実在する .txt** をここに1つ置いてある。

無いファイルで 404 を確かめても、拡張子で落ちたのか
ファイルが無かったのかが区別できない。selfcheck.py はこのファイルを使う。
